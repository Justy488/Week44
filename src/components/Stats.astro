<section class="stats">
  <div class="stats-content">
    <h2 class="title">Statistics</h2>

    <div class="stat-circle">41%</div>

    <h3>HAR I NOGEN GRAD TALT OM DØDEN</h3>

    <p>Mange taler kun i begrænset grad om døden. Derfor skaber vi et rum, hvor samtalen kan begynde.</p>

    <a href="/statistik" class="btn">Se alle stats <span>→</span></a>

    <img src="/pictures/peoplegraf01.png" alt="Statistik kort" class="stat-card mobile-card" />
  </div>

  <img src="/pictures/peoplegraf01.png" alt="Statistik kort" class="stat-card desktop-card" />
  <img src="/pictures/hourglassshape.png" alt="" class="decor-shape" />
</section>

<style>
  .stats {
    position: relative;
    overflow: hidden;
    background: #fff;
    padding: 2.5rem 1.5rem 3rem;
    border-top: 2px solid #a67c44;
    border-bottom: 2px solid #a67c44;
  }

  .stats-content {
    position: relative;
    z-index: 2;
    max-width: 420px;
    margin: 50px;
  }

  .title {
    font-size: 4rem;
    line-height: 0.95;
    font-weight: 400;
    margin-bottom: 1.8rem;
  }

  .stat-circle {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    background: #c0dae0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    font-weight: 500;
    color: #000;
    margin: 0 auto 1.8rem;
  }

  h3 {
    color: #9b7442;
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.8rem;
    line-height: 1.2;
  }

  p {
    font-size: 1rem;
    line-height: 1.35;
    max-width: 320px;
    margin-bottom: 1.5rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    padding: 0.9rem 1.3rem;
    background: #c0dae0;
    color: #000;
    border: 1px solid #000;
    border-radius: 999px;
    text-decoration: none;
    font-size: 1rem;
  }
  .btn:hover {
    background: #ebfbff;
    color: #000;
  }

  .stat-card {
    display: block;
    width: 290px;
    max-width: 100%;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.12);
    position: relative;
    z-index: 2;
  }

  .desktop-card {
    display: none;
  }

  .mobile-card {
    margin-top: 0.5rem;
  }

  .decor-shape {
    position: absolute;
    right: 0;
    top: 0;
    width: 380px;
    height: auto;
    max-height: 100%;
    z-index: 1;
    pointer-events: none;
  }

  @media (min-width: 900px) {
    .stats {
      min-width: 900px;
      padding: 2rem 2rem 2rem;
      min-height: 500px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: start;
    }

    .stats-content {
      max-width: 520px;
      padding-left: 1rem;
    }

    .title {
      font-size: 4.5rem;
      margin-bottom: 1.5rem;
    }

    .stat-circle {
      width: 130px;
      height: 130px;
      font-size: 4.2rem;
      margin: 0 0 1.8rem 7rem;
    }

    h3 {
      font-size: 1.5rem;
      margin-bottom: 0.8rem;
      max-width: 430px;
    }

    p {
      max-width: 360px;
      margin-bottom: 1.4rem;
      line-height: 1.5;
      font-size: 1.3rem;
    }

    .mobile-card {
      display: none;
    }

    .desktop-card {
      display: block;
      position: absolute;
      top: 105px;
      right: 140px;
      width: 280px;
    }

    .decor-shape {
      right: 0;
      top: 0;
      width: 380px;
      height: 100%;
    }
    .btn {
      font-size: 1.3rem;
    }
  }
</style>
